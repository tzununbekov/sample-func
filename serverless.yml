service: time

provider:
  name: kubeless
  runtime: python2.7

plugins:
  - serverless-kubeless

functions:
  hellowithdata:
    handler: hellowithdata.handler
    events:
      - http:
          method: get
